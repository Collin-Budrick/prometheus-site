import LoginPage from '../[locale]/login/index'
import { LocaleEntry, usePreferredLocale } from '../_shared/locale/entry'

export { head } from '../[locale]/login/index'

export { usePreferredLocale }

export default LocaleEntry(LoginPage)
