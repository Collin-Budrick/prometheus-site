# Template Bundle Cookbook

Generated by `bun run template:sync`.

Use this cookbook when you need to disable, replace, or fork a built-in bundle safely.

## auth

Self-hosted Better Auth on Convex, bootstrap tokens, and the login route.

- Depends on: none
- Routes: `/login`
- Owners: `template`, `auth`
- Required secrets: `AUTH_BOOTSTRAP_PRIVATE_KEY`, `BETTER_AUTH_SECRET`
- Quality gates: `build`, `typecheck`, `browser`
- Adapters: `Convex`, `Better Auth`
- Migrations: Better Auth base path, JWT issuer/audience, and JWKS settings must stay in sync across Convex, the site bundle, the Rust API, and the SpacetimeDB module.

## store

Catalog, cart, and store mutation routes backed by starter data or live inventory.

- Depends on: `auth`
- Routes: `/store`
- Owners: `template`, `store`
- Required secrets: none
- Quality gates: `build`, `typecheck`, `browser`
- Adapters: `SpaceTimeDB`, `starter data`
- Migrations: Store bundle expects matching catalog and mutation support.

## lab

Reusable experimentation surface for prototyping new fragments and UI ideas.

- Depends on: none
- Routes: `/lab`
- Owners: `template`
- Required secrets: none
- Quality gates: `build`, `typecheck`
- Adapters: none
- Migrations: Lab is intentionally starter-safe and should remain optional.

## messaging

Protected chat routes, P2P mailbox flows, and push notifications.

- Depends on: `auth`
- Routes: `/chat`
- Owners: `template`, `messaging`
- Required secrets: `PUSH_VAPID_PUBLIC_KEY`, `PUSH_VAPID_PRIVATE_KEY`, `PUSH_VAPID_SUBJECT`, `PUSH_FCM_PROJECT_ID`, `PUSH_FCM_CLIENT_EMAIL`, `PUSH_FCM_PRIVATE_KEY`, `PUSH_APNS_KEY_ID`, `PUSH_APNS_TEAM_ID`, `PUSH_APNS_BUNDLE_ID`, `PUSH_APNS_PRIVATE_KEY`, `PUSH_APNS_USE_SANDBOX`
- Quality gates: `build`, `typecheck`, `browser`
- Adapters: `Push API`, `relay signaling`, `SpaceTimeDB`
- Migrations: Push provider env keys must match the enabled delivery adapters.

## account

Profile, settings, and dashboard routes for authenticated users.

- Depends on: `auth`
- Routes: `/profile`, `/settings`, `/dashboard`
- Owners: `template`, `account`
- Required secrets: none
- Quality gates: `build`, `typecheck`, `browser`
- Adapters: none
- Migrations: Account routes assume auth bundle remains enabled.

## demo-home

Shared home route composition and starter-safe fragment demos.

- Depends on: none
- Routes: `/`
- Owners: `template`
- Required secrets: none
- Quality gates: `build`, `typecheck`, `browser`, `storybook`
- Adapters: none
- Migrations: Starter-safe home copy should remain editable through template-config.

## demo-react

Server-only React authoring demo compiled into binary fragments.

- Depends on: `demo-home`
- Routes: none
- Owners: `template`
- Required secrets: none
- Quality gates: `build`, `typecheck`, `storybook`
- Adapters: `React authoring`
- Migrations: Keep React authoring server-only and out of the client ownership path.

## demo-preact

Reusable client island demo for progressive enhancement.

- Depends on: `demo-home`
- Routes: none
- Owners: `template`
- Required secrets: none
- Quality gates: `build`, `typecheck`, `storybook`
- Adapters: `Preact island`
- Migrations: Islands should stay isolated from shell ownership.

## demo-wasm

WebAssembly-backed fragment rendering demo content.

- Depends on: `demo-home`
- Routes: none
- Owners: `template`
- Required secrets: none
- Quality gates: `build`, `typecheck`, `storybook`
- Adapters: `WASM renderer`
- Migrations: WASM demo assets must remain deterministic across builds.

## realtime

Realtime transport, collaboration, WebTransport, and signaling services.

- Depends on: none
- Routes: none
- Owners: `template`, `realtime`
- Required secrets: none
- Quality gates: `build`, `typecheck`, `browser`
- Adapters: `WebTransport`, `Garnet`, `relay signaling`
- Migrations: Realtime profile changes require matching API, relay, and client env updates.

## pwa

Offline route, manifest, and service worker integration.

- Depends on: none
- Routes: `/offline`
- Owners: `template`
- Required secrets: none
- Quality gates: `build`, `browser`, `lighthouse`
- Adapters: none
- Migrations: Service worker and manifest changes should ship together.

## analytics

Analytics beacon, Highlight, and Partytown integrations.

- Depends on: none
- Routes: none
- Owners: `template`
- Required secrets: none
- Quality gates: `build`, `typecheck`, `lighthouse`
- Adapters: `Highlight`, `Partytown`, `custom beacon`
- Migrations: Analytics integrations stay opt-in and should never become hard requirements.

## native

Native shell affordances and mobile-only entry points.

- Depends on: none
- Routes: none
- Owners: `template`
- Required secrets: none
- Quality gates: `build`, `typecheck`, `desktop`
- Adapters: `Electrobun`
- Migrations: Native affordances must preserve browser fallbacks.
