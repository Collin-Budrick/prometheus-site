http:
  routers:
    prometheus-prod-web:
      rule: Host(`prometheus.test`)
      entryPoints:
        - websecure
      service: prometheus-prod-web
      tls: {}
      priority: 1
    prometheus-prod-api:
      rule: Host(`prometheus.test`) && PathPrefix(`/api`)
      entryPoints:
        - websecure
      service: prometheus-prod-api
      middlewares:
        - prometheus-prod-api-strip
      tls: {}
      priority: 10
  services:
    prometheus-prod-web:
      loadBalancer:
        servers:
          - url: http://web:4173
    prometheus-prod-api:
      loadBalancer:
        servers:
          - url: http://api:4000
  middlewares:
    prometheus-prod-api-strip:
      stripPrefix:
        prefixes:
          - /api
