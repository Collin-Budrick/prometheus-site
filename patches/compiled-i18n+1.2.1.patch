diff --git a/node_modules/compiled-i18n/dist/transform-localize.mjs b/node_modules/compiled-i18n/dist/transform-localize.mjs
index d05ac79..0adba7c 100644
--- a/node_modules/compiled-i18n/dist/transform-localize.mjs
+++ b/node_modules/compiled-i18n/dist/transform-localize.mjs
@@ -210,7 +210,18 @@ const replaceGlobals = ({
     if (!argExprs.length) {
       throw new Error(`No arguments found for __$LOCALIZE$__`);
     }
-    const key = JSON.parse(argExprs.shift());
+    const rawArg = argExprs.shift();
+    let key;
+    try {
+      key = JSON.parse(rawArg);
+    } catch (err) {
+      const trimmed = typeof rawArg === "string" ? rawArg.trim().replace(/,$/, "") : "";
+      if (trimmed.startsWith("`") && trimmed.endsWith("`")) {
+        key = trimmed.slice(1, -1);
+      } else {
+        throw err;
+      }
+    }
     const tr = getTr(key, locale, translations);
     code = code.slice(0, startIndex) + makeTranslatedExpr(tr, argExprs) + chunk.slice(i + 1);
   }

diff --git a/node_modules/compiled-i18n/dist/vite.mjs b/node_modules/compiled-i18n/dist/vite.mjs
index 4d049ac..31184a5 100644
--- a/node_modules/compiled-i18n/dist/vite.mjs
+++ b/node_modules/compiled-i18n/dist/vite.mjs
@@ -1,67 +1,68 @@
 import { resolve, sep } from "node:path";
 import fs from "node:fs";
 import { replaceGlobals, transformLocalize } from "./transform-localize.mjs";
 const sortObject = (o) => Object.fromEntries(
   Object.entries(o).sort(
     ([a], [b]) => a.localeCompare(b, "en", { sensitivity: "base" })
   )
 );
 function i18nPlugin(options = {}) {
   const {
     localesDir = "i18n",
     babelPlugins,
     addMissing = true,
     removeUnusedKeys = false,
-    tabs
+    tabs,
+    lazy = false
   } = options;
   let assetsDir = options.assetsDir;
   if (assetsDir && !assetsDir.endsWith("/")) assetsDir += "/";
   const locales = options.locales || ["en"];
   const defaultLocale = options.defaultLocale || locales[0];
   const localeNames = {};
   const localesDirAbs = resolve(localesDir);
   const localesDirNode = sep !== "/" ? localesDirAbs.replaceAll(sep, "/") : localesDirAbs;
   let shouldInline = false;
   let translations;
   let hasTabs;
   let allKeys;
   let pluralKeys;
   return [
     {
       name: "i18n",
       enforce: "pre",
       async config() {
         const updatedViteConfig = {
           optimizeDeps: {
             // Make sure we process our virtual files
             exclude: ["compiled-i18n"]
           },
           ssr: {
             // Make sure we bundle our module
             noExternal: ["compiled-i18n"]
           }
         };
         return updatedViteConfig;
       },
-      configResolved(config) {
-        shouldInline = !config.build.ssr && config.mode === "production";
+        configResolved(config) {
+          shouldInline = !lazy && !config.build.ssr && config.mode === "production";
         if (!assetsDir && config.plugins.some((p) => p.name === "vite-plugin-qwik"))
           assetsDir = "build/";
       },
       buildStart() {
         fs.mkdirSync(localesDirAbs, { recursive: true });
         const fallbacks = {};
         translations = {};
         hasTabs = {};
         allKeys = /* @__PURE__ */ new Set();
         pluralKeys = /* @__PURE__ */ new Set();
         for (const locale of locales) {
           const match = /^([a-z]{2})([_-]([A-Z]{2}))?$/.exec(locale);
           if (!match)
             throw new Error(
               `Invalid locale: ${locale} (does not match xx or xx_XX))`
             );
           const localeFile = resolve(localesDirAbs, `${locale}.json`);
           let data;
           if (fs.existsSync(localeFile)) {
             const text = fs.readFileSync(localeFile, "utf8");
@@ -99,40 +100,57 @@ function i18nPlugin(options = {}) {
               fs.writeFileSync(
                 localeFile,
                 JSON.stringify(data, null, tabs ? "	" : 2)
               );
           }
           localeNames[locale] = data.name;
           translations[locale] = data;
           for (const [key, tr] of Object.entries(data.translations))
             if (tr && typeof tr === "object") pluralKeys.add(key);
         }
       },
       // Redirect to our virtual data files
       async resolveId(id) {
         if (id.startsWith("@i18n/__locales")) return "\0i18n-locales.js";
         if (id.startsWith("@i18n/__data")) return "\0i18n-data.js";
         if (id.startsWith("@i18n/__state")) return "\0i18n-state.js";
       },
       // Load our virtual data files
       async load(id) {
         if (id === "\0i18n-locales.js") {
+          if (lazy) {
+            return `
+const store = {};
+
+export const loadLocaleData = async (locale) => {
+  switch (locale) {
+${locales.map((l) => `    case '${l}':
+      if (!store[locale]) store[locale] = (await import('${localesDirNode}/${l}.json')).default;
+      return store[locale];`).join("\n")}
+    default:
+      throw new Error('Invalid locale ' + locale);
+  }
+};
+
+export default store;
+`;
+          }
           return `
 /**
  * This file was generated by compiled-i18n.
  *
  * For server builds, it contains all translations. For client builds, it is
  * empty, and translations need to be loaded dynamically.
  */
 ${shouldInline ? `export default {"__$LOCALE$__": {translations: {}}}` : `
 ${locales.map((l, i) => `import _${i} from '${localesDirNode}/${l}.json'`).join("\n")}
 
 export default {
 ${locales.map((l, i) => `  "${l}": _${i},`).join("\n")}
 }
 `}
 `;
         }
         if (id === "\0i18n-data.js") {
           return `
 /** This file is generated at build time by \`compiled-i18n\`. */
 /** @type {import('compiled-i18n').Locale[]} */
